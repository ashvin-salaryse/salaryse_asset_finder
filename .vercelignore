# Vercel only HOSTS the prebuilt gallery. The Python scripts are build tooling
# (run in GitHub Actions, not on Vercel) — excluding them stops Vercel from
# trying to build a Python app.
*
!asset_gallery.html
!vercel.json

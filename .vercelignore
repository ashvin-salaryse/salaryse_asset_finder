# Exclude the Python build tooling so Vercel doesn't try to build a Python app.
# (asset_gallery.html + vercel.json stay, so the static site actually deploys.)
*.py
requirements.txt
refresh.sh
lottie.min.js
venv
.github
app-repo-notifier
